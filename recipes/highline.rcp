(:name highline
       :description "minor mode to highlight current line in buffer"
       :type emacsmirror
       :prepare (defun highline-mode-on ()
                  (interactive)
                  (highline-mode 1)))
