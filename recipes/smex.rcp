(:name smex
       :description "M-x interface with Ido-style fuzzy matching."
       :type github
       :username "nonsequitur"
       :reponame "smex"
       :features smex
       :post-init (lambda ()
            (smex-initialize)))

